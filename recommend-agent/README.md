# Restaurant Recommend Agent (Scheme A)

FastAPI + LangGraph 多智能体推荐服务，通过 HTTP 调用 `smart-ordering-system` 的菜品与推荐 API。

详见 [docs/SCHEME_A.md](docs/SCHEME_A.md)。

```bash
uvicorn app.main:app --host 127.0.0.1 --port 8001
```
